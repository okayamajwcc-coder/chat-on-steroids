# Combined working-tree publication, 20 September 2026

The requested commit, pull request, merge and local installation use one captured
working tree. Integration starts from public main 8f76ccc7 and
preserves its existing fixes. The shared checkout's private ancestry and original
reporter exports are excluded from the public branch.

## Included behavior

The combined changes add helper/recorder 19, live shell request and public activity
projection, exact native anchors for chronological tools, and rendering custody
while fresh background workers mount their editors. Response observers can refresh
and retire without duplicate active readers; native resume streams retain their
complete request identities.

Astra Goal and Loop preserve their selected decision policies and delivery timing.
Queued inputs retain the exact turn they awaited when its final report arrives
late. Replacement chats carry the saved task plan within the same message budget.
Tunnel authentication errors require an actual control-plane rejection. Usage adds
verified native-message counts with local calendar boundaries and a saved weekday.

## Fresh validation before publication

On the isolated combined candidate, the complete verify:ci command passed with
two Vitest workers: 5,654 tests passed and 45 opt-in tests skipped, including the
separately executed MCP shutdown suite. Dependency, privacy, notices, typechecking
and Electron resolution checks passed. Production build passed.

Real Chromium/Electron fixtures passed for shell metadata, cold model selection,
literal insertion, three exact send/final receipts, minimized worker hydration,
weekly-count keyboard activation and persistence, and dropdown layout. Usage and
dropdown screenshots were inspected at normal and enlarged layouts. These are
isolated fixtures, not new acceptance evidence from the affected reporter account.

Runtime verification used tree a3321f4b0cb6d489a508f31109020b1b7b6121e0; this worklog is
the only added file after those checks. All runtime, test and script contents
retain the captured source. Existing diagnostics credit remains in CONTRIBUTORS.

## Installation gates

The package is built from the resulting public commit. Its complete payload,
extension and bundled outputs are hashed before installation. Installation requires
successful PR checks on Windows, macOS and Linux, successful CI for the merged
commit, and equality of that commit's tree with the package's source. Local receipts
then record installer exit, app reconnection, native smoke and installed byte
comparison. This entry does not claim those later steps have already completed.
